# RunAnywhere Web SDK key (optional for local inference).
# Get a key from https://runanywhere.ai — inference still runs on-device.
# Do NOT commit .env or put real keys in the repo.

VITE_RUNANYWHEREAI_KEY=your_key_here
